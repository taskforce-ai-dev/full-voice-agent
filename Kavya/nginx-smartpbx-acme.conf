# Temporary HTTP-only ACME bootstrap. No application proxy is exposed.
server {
    listen 80;
    listen [::]:80;
    server_name smartpbx-kavya.taskforceai.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 404;
    }
}
