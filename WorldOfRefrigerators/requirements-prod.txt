# Core server
fastapi>=0.104.0
uvicorn[standard]>=0.52.0
websockets>=17.0.1

# AI / LLM
anthropic>=0.40.0
openai>=1.0.0

# Telephony
# Pinned to 7.x so AccessToken.to_jwt() returns a str (used by /api/voice-token).
twilio>=9.10.9,<10.0

# Knowledge base / RAG
chromadb>=0.4.0
sentence-transformers>=2.2.0

# STT (Media Streams) — Google (default) + Azure (STT_PROVIDER=azure)
google-cloud-speech>=2.0.0
azure-cognitiveservices-speech>=1.34.0
# audioop (mulaw→PCM for Azure STT + audio dumps) is stdlib on the 3.11 image;
# backport it only where it was removed (3.13+).
audioop-lts; python_version >= "3.13"

# Gemini native SDK
google-genai>=2.15.0

# HTTP client
httpx>=0.25.0

# Form parsing (required for request.form() — Twilio webhook body parsing)
python-multipart>=0.0.32

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0

# PDF support for knowledge base (optional)
PyPDF2>=3.0.1

# Error tracking (optional; activated by SENTRY_DSN)
sentry-sdk>=2,<3
