# Core server
fastapi>=0.104.0
uvicorn[standard]>=0.52.0
websockets>=12.0

# AI / LLM
anthropic>=0.120.2
openai>=1.0.0

# Telephony
twilio>=9.0.0

# Knowledge base / RAG
chromadb>=0.4.0
sentence-transformers>=2.2.0
numpy>=1.24
# kb/ models the query filters and listings with pydantic and uses the v2 API
# (model_dump). It arrived transitively via fastapi/chromadb; pin the major so a
# rebuild can't silently resolve a version the KB can't run on.
pydantic>=2,<3

# STT
google-cloud-speech>=2.0.0
azure-cognitiveservices-speech>=1.51.1

# Gemini native SDK
google-genai>=1.0.0

# HTTP client
httpx>=0.25.0

# Form parsing (required for request.form() — Twilio webhook body parsing)
python-multipart>=0.0.7

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0

# PDF support for knowledge base (optional)
PyPDF2>=3.0.0

# Error tracking (optional; activated by SENTRY_DSN)
sentry-sdk>=2,<3
