# Core server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# Upper bound is NOT optional: google-genai (below) requires websockets<17.0.
# This file previously said `websockets>=17.0`, which combined with google-genai
# made the requirements unsatisfiable — Sofia could not build at all. Every
# other agent in this repo resolves to websockets 16.1.1 for the same reason.
websockets>=13.0,<17.0

# AI / LLM
anthropic>=0.120.2
openai>=1.0.0

# Telephony
twilio>=9.10.9

# Knowledge base / RAG
chromadb>=1.5.9
sentence-transformers>=5.6.1

# STT (future Media Streams)
google-cloud-speech>=2.40.0

# Gemini native SDK
google-genai>=2.16.0

# HTTP client
httpx>=0.25.0

# Form parsing (required for request.form() — Twilio webhook body parsing)
python-multipart>=0.0.32

# Utilities
python-dotenv>=1.2.2
aiohttp>=3.14.3

# PDF support for knowledge base (optional)
PyPDF2>=3.0.1

# Error tracking (optional; activated by SENTRY_DSN)
sentry-sdk>=2,<3
