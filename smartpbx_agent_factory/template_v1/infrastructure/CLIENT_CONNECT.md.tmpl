# Client Connect candidate sheet

REVIEW-ONLY

activation: blocked

The proposed media endpoint is `wss://{{smartpbx_hostname}}/ws/v1/smartpbx/media`.
Use the `X-SmartPBX-Token` request header. No endpoint is active from this candidate;
the status path requires the same header and remains inaccessible without it.
