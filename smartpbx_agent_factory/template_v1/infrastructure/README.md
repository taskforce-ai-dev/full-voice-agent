# Infrastructure template seam

Infrastructure templates are intentionally empty of deployment values until
immutable SmartPBX provenance is approved. No production host, credential, or
client identity belongs in this directory.
