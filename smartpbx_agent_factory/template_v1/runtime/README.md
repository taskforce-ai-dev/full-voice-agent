# Runtime template seam

This directory is intentionally client-neutral. Runtime source is not frozen
until an immutable deployed-image revision and digest are established from
approved evidence. The checked-in provenance gate is currently blocked.
